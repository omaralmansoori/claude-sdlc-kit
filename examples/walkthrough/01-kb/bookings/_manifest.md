# bookings — conversion manifest

Documents in this area: 2

## Conversion method

| Source format | Converter |
| --- | --- |
| .md / .txt | passthrough (this synthetic corpus is already markdown) |

> In a real corpus this table also lists markitdown (`.docx/.pptx/.pdf/.html`) and the custom
> `.xlsx`/`.vsdx` extractors. This worked example ships markdown originals so the whole slice is
> reproducible without binary fixtures.

## Quality caveats (curated)

- [x] OCR'd scans: **n/a** — no scanned sources in this synthetic corpus.
- [x] Bilingual content: **n/a** — English only.
- [x] Process flows: **n/a** — no diagrams in this slice (a real one would dual-capture step text + a rendered `.png`).
- [x] Excluded/superseded originals: none present.
- [x] Duplicate documents: none — both docs are canonical.
